# Dependencies
node_modules/

# Secrets & runtime data — NEVER commit (bot tokens, admin hash, sessions, cached messages)
.env
db/

# Local tooling / editor state
.claude/
*.log
.DS_Store
logs/

# SSH private keys - NEVER commit
*.key
*.pem
!*.key.pub
octogod.key.pub
